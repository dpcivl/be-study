package com.study;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

class AppTest {
    @Test
    void appHasABook() {
        // 지금은 테스트를 배우는 단계가 아니라, 빌드가 통과하도록 최소한만 둠
        assertTrue(true);
    }
}